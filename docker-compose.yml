services:
  app:
    build: .
    ports:
      - "80:8080"
    environment:
      - APP_USER=There
    depends_on:
      - redis

  redis:
    image: redis:latest
    volumes:
      - redis_data:/data

volumes:
  redis_data:
